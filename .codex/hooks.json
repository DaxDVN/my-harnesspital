{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'd=\"$PWD\"; i=0; while [ $i -lt 12 ]; do g=\"$d/engine/hooks/myhospital_guard.py\"; if [ -f \"$g\" ]; then exec python3 \"$g\"; fi; p=$(dirname \"$d\"); if [ \"$p\" = \"$d\" ]; then break; fi; d=\"$p\"; i=$((i+1)); done; exit 0'",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
