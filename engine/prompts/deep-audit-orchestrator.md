# deep-audit-orchestrator — archived full prompt stub

Use `engine/workflows/deep-review/{protocol,checklist,findings-schema}.md` for normal review routing.

The former self-contained mega prompt was archived to:

```text
docs/harness/archive/prompts/deep-audit-orchestrator.full.md
```

Load the archived full prompt only when the owner explicitly wants a fresh-agent, copy-paste style audit
session that is intentionally self-contained and token-heavy. Daily harness work should use `mh-review` /
`deep-review` docs instead.
