# GUIDELINE — archived maintenance guide stub

The former long all-in-one harness guideline was archived to:

```text
docs/harness/archive/prompts/GUIDELINE.full.md
```

For daily work, use:

- `AGENTS.md` for boot rules;
- `scripts/harness_router.py "<prompt>"` for routing;
- `engine/REGISTRY.md` for inventory;
- `scripts/harness_context_audit.py scan` for context/token footprint;
- `scripts/harness_ready.py` for readiness validation.

Load the archived full guide only for historical audit or when the owner explicitly asks for the old
full-maintenance prompt.
