# Deprecated PowerShell Scripts

These files are archived from the old Windows workflow for reference only.

Do not use `powershell`, `pwsh`, or these `*.ps1` files as the primary runtime in Linux sessions. Current tooling lives in:

- `scripts/worktree.py`
- `scripts/careflow_ba_fixes_smoke.py`
- `.claude/hooks/myhospital_guard.py`
- `.claude/hooks/graphify_stale_check.py`
